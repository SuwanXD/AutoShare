# Instalasi Tools
$ pkg update && pkg upgrade

$ pkg install python

$ pip install requests

$ pip install bs4

$ pip install rich

$ git clone https://github.com/SuwanXD/Auto-Share.git

$ cd Auto-Share

$ ls

$ git pull

$ python Run.py

